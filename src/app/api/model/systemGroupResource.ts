/**
 * API
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 0.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
import { Type, Expose } from 'class-transformer';
// @dynamic

import { SystemGroup } from './systemGroup';

/**
 * model.mustache
 *
 * SystemGroupResource
 */
import { Observable }                                        from 'rxjs';
import { AbstractControl, FormControl, FormGroup, FormArray, Validators }                            from '@angular/forms';

export class SystemGroupResource { 


    @Expose()
    data!: SystemGroup;

    /**
     * datatype: SystemGroup
     * datatypeWithEnum: SystemGroup
     * data: SystemGroup   
     */

    // validations?: Map<string, Array<{[key: string]: string}>> = new Map<string, Array<{[key: string]: string}>>();

    constructor(init: Partial<SystemGroupResource> = {}) {
         
                        this.data = new SystemGroup(init.data || {})
    }

    static   getForm(data?: SystemGroupResource | SystemGroupResource[] | null): FormGroup {


        if(!data) {
            return SystemGroupResource.getFormGroup(new SystemGroupResource());
        }

         if(Array.isArray(data)) {
            let arrayForm = new FormArray<any>([]);
            
            if(data.length > 0) {
                arrayForm =  new FormArray<any>(data.map(item => SystemGroupResource.getFormGroup(item)));                
            }
            return new FormGroup({
                arrayForm
            });        
        } else {
             return  SystemGroupResource.getFormGroup(data);
        }       
     
    }

     /**
   * Get FormGroup
   *
   * @param {object} data
   * @returns {FormGroup}
   */
  static   getFormGroup(data?: SystemGroupResource): FormGroup {
       
        return new FormGroup({           
                        data: (() => { 
                            const fg = SystemGroup.getForm(data?.data);
                            fg.addValidators([]);
                            return fg;
                        })()
        });
    }
  

}