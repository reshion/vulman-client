import { BaseSeverityHighlightDirective } from './base-severity-highlight.directive';

describe('BaseSeverityHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseSeverityHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
